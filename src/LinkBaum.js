export class LinkBaum {

    constructor( titel, beschreibung, linkArray ) {

        this.titel         = titel;
        this.beschreibung  = beschreibung;
        this.linkArray     = linkArray || [];
    }
}